[package]
name = "uto-logger"
version = "0.1.0"
edition = "2021"
description = "Structured logging and loader utilities for UTO"

[dependencies]
indicatif = "0.17"
once_cell = "1"
tracing = "0.1"
tracing-log = "0.2"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "time"] }

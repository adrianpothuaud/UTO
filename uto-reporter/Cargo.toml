[package]
name = "uto-reporter"
version = "0.1.0"
edition = "2021"
description = "Structured uto-report/v1 JSON and HTML reporting for UTO"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
